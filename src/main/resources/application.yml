#spring:
#  sql:
#    init:
#      mode: always
#  application:
#    name: task-api
#  jpa:
#    hibernate:
#      ddl-auto: create
#    open-in-view: false
#    show-sql: true
#    defer-datasource-initialization: true
#    properties:
#      hibernate:
#        format_sql: true
#        use_sql_comments: true
#        highlight_sql: true
#        auto_quote_keyword: true
#logging:
#  level:
#    org.hibernate.orm.jdbc.bind: trace
#    org.springframework.transaction.interceptor: trace
#

spring:
  datasource:
    url: jdbc:mysql://s4.java21.net:13306/nhn_academy_118
    username: nhn_academy_118
    password: nP3xraub!
    driver-class-name: com.mysql.cj.jdbc.Driver

  sql:
    init:
      mode: never  # SQL 스크립트 자동 실행 안 함 (필요 시 always)

  application:
    name: task-api

  jpa:
    hibernate:
      ddl-auto: update  # 기존 테이블 유지하면서 수정 (개발용)
    open-in-view: false
    show-sql: true
    defer-datasource-initialization: true
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        highlight_sql: true
        auto_quote_keyword: true

logging:
  level:
    org.hibernate.orm.jdbc.bind: trace
    org.springframework.transaction.interceptor: trace
